import { Component, Input, OnInit } from '@angular/core';

@Component({
  selector: 'home-route',
  template: '<home-component class="home-component"></home-component>',
  styleUrls: ['./routes.css']
})

export class HomeRoutesComponent {
  constructor() {
  }
}